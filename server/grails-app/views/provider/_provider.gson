import demowebcomparator.Provider

model {
	Provider provider
}

json {
    id provider.id

    code provider.code

    name provider.name

    summary provider.summary

    isActive provider.isActive
}
